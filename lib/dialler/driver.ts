/**
 * The {@link DiallerDriver} contract lives in `./types` alongside the value
 * types it depends on (`Call`, `CallControl`, `CallHandlers`). This module
 * re-exports it so importers can mirror the email package's
 * `@/lib/email/driver` entry point.
 */
export type { DiallerDriver } from '@/lib/dialler/types';
